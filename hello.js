const math = require('./math') // require func we can import modules,

// console.log(`Math Value is ${math.addFn(4,7)}`)
// console.log(`Math Value is ${math.subFn(4,7)}`)

// const {addFn, subFn} = require("./math") // destructuring

// console.log(`Math Value is ${addFn(99,1)}`)
// console.log(`Math Value is ${subFn(99,1)}`)

console.log(math)
// since we have not named the function so these functions are anonymous

// Download XAMPP

console.log(`Math Value is ${math.add(4,7)}`)
console.log(`Math Value is ${math.sub(4,7)}`)